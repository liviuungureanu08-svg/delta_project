providers:
  video:
    default: mock
    available:
      mock:
        type: mock
        cost_per_second_usd: 0.0
      higgsfield:
        type: api
        cost_per_second_usd: null   # set when known; never hard-code
        api_base: null              # set via environment variable
        enabled: false

  image:
    default: mock
    available:
      mock:
        type: mock
        cost_per_image_usd: 0.0
      flux:
        type: api
        cost_per_image_usd: null
        enabled: false

  tts:
    default: mock
    available:
      mock:
        type: mock
        cost_per_second_usd: 0.0
      elevenlabs:
        type: api
        cost_per_second_usd: null
        enabled: false
